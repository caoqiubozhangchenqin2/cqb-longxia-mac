# 2026-04-05 周日

## 今日事项

### 🤖 多 Agent 体系文档层恢复
- 用户确认：历史上使用的是一套“总管 + 7 个专项 Agent”的结构。
- 已确认的专项 Agent 包括：
  - 军情六处（info-researcher）
  - 身高记录师（height-recorder）
  - 球队经理（team-manager）
  - 课程顾问（course-advisor）
  - 基金助手（fund-assistant）
  - 招聘 HR（recruiter）
  - 设计师（design-assistant）
- 已核实：当前 OpenClaw 运行时实际注册仅剩 `main`，无 routing bindings；但各专项 Agent 目录与大量资料仍保留在 `agents/` 下。
- 用户选择仅执行“方案 B（文档层恢复）”，不立即恢复运行层。

### 🗂️ 已完成
- 已新增总索引文件：`backup/agent-system-index.md`
- 该文件统一整理了：
  - 各 Agent 的中文名 / 英文目录名
  - 各自职责范围
  - 主备份入口
  - 建议运行台账
  - 当前适用任务
  - main 的总调度口径

### 📌 当前共识
- 后续即便运行时暂时只有 `main`，也默认按旧七大专项 Agent 的职责边界来理解和处理任务。
- 当前最完整、优先级最高的三大专项仍是：基金、课程、球队。

### 📝 待跟进
- 如后续需要，可继续补充：
  1. 各 Agent 的 cron 任务索引
  2. 各 Agent 的路由 / 群绑定关系
  3. 各 Agent 的外发权限边界
  4. 设计师的专用职责文档
